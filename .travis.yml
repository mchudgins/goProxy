language: go
sudo: required
services:
  - docker

go:
  - 1.x

go_import_path: github.com/mchudgins/goProxy

script:  go test -v ./... && CGO_ENABLED=0 go build -o docker/goProxy main.go && sudo docker build -t mchudgins/goproxy docker
